# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gemini

# ── Local development database ────────────────────────────────────────────────
# cloud_db.json is a local-only dev database. It is auto-generated from
# cloud_db.json.example on first run. Never commit it — production tenants
# must register through the app. Only the .example template is committed.
cloud_db.json
scratch/

# Environment files and credentials
.env
.env.local
.env.*.local
.env.production
.env.staging

